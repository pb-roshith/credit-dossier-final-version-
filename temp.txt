lets try different approach for RAG, use mistral document processing capabilities. Use mistral library for storing the files and then create 16 section agent on mistral with specilized instruction for generating the response for that section and also ensure agent response doesn't hallucinate. also make sure instruction for agents should be highly optimized to use highly relevent content from mistral library. make approproiate changes on ui also to upload all the document added from every section to library and remove the feature for selecting document in every section and show the existing document in library in every sectiona and allow user to add new document.